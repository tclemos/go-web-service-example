version: "1"
packages:
  - name: "db"
    path: "./db"
    queries: "./queries/"
    schema: "./migrations/"
    engine: "postgresql"
    emit_prepared_queries: true
    emit_json_tags: true
    emit_interface: true
